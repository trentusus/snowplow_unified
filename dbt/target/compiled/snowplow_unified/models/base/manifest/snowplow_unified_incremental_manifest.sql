






        with prep as (
        select
            cast(null as 
    string
) model,
            cast('1970-01-01' as timestamp) as last_success
        )

        select *

        from prep
        where false
    